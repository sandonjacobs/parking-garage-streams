spring:
  kafka:
    bootstrap-servers: localhost:9092
    properties:
      "[schema.registry.url]": mock://test
#    streams:
#      application-id: test-parking-streams
#      properties:
#        auto.offset.reset: earliest
#        schema.registry.url: mock://test


logging:
  level:
    io.sandonjacobs.parking.kstreams: DEBUG
    org.apache.kafka.streams: INFO 